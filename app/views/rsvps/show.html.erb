<%= link_to @rsvp.user_name, user_path(@rsvp.user) %><br>
	<% if current_user.rsvps.empty? %>
		No RSVP.
	<% else %> 
		
		Title: " <%= @rsvp.exhibition.name.capitalize %> " - status: <%= @rsvp.status %> 
	<% end %>